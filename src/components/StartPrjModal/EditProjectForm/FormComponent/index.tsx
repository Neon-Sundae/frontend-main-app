import styles from './index.module.scss';

const FormComponent = () => {
  return (
    <div className={styles['edit-project-form-container']}>Form Component</div>
  );
};

export default FormComponent;
